ul.nav.nav-tabs
  li.nab-item
    = link_to root_path(tab: 'all_articles'),
              class: tab_active?('all_articles', initial_tab: true) do
      small 全ての投稿
      span.badge#all-count
        = @articles_count
  li.nab-item
    = link_to root_path(tab: 'my_articles'),
              class: tab_active?('my_articles') do
      small 自分の投稿
      span.badge#mine-count
        = @user_articles_count
  li.nab-item
    = link_to root_path(tab: 'likes'),
              class: tab_active?('likes') do
      small お気に入り
      span.badge#like-count
        = @liked_articles_count
  li.nab-item
    = link_to root_path(tab: 'followers'),
              class: tab_active?('followers') do
      small フォロワー
      span.badge#followers-count
        = @followers_count
  li.nab-item
    = link_to root_path(tab: 'following'),
              class: tab_active?('following') do
      small フォロワー
      span.badge#following-count
        = @following_count
